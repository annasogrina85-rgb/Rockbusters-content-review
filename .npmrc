# Skip devDependencies (playwright + browser binaries) when running npm install.
# This keeps the CCR morning agent fast — playwright is only needed locally for image generation.
# To install playwright locally: npm install --include=dev
omit=dev
